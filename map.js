/*
    * Exemplo:
    - Multiplique os valores do array  "values" por 2;
    - Atribua esse valores à um novo array;
    - Exiba os valores desse novo array no console;
 */

const values = [20, 2, 15, 18, 5, 53, 16];

const multipliedValues = values.map((value) => value * 2);
console.log(multipliedValues);